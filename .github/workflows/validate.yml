name: Validate Supper
on:
  push:
    branches: [main, supper-household-update]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: validate-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  core:
    runs-on: macos-15
    timeout-minutes: 5
    outputs:
      ui-matrix: ${{ steps.suites.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - name: Validate UI coverage and select suites
        id: suites
        run: |
          matrix=$(python3 .github/scripts/ui_test_shards.py --matrix)
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"
      - name: Validate simulator retry classification
        run: python3 -B -m unittest discover -s .github/scripts -p 'test_*.py'
      - name: Select Xcode 26
        run: sudo xcode-select -s /Applications/Xcode_26.0.app/Contents/Developer
      - name: Domain and persistence regression tests
        run: swift test
  regression:
    needs: core
    runs-on: macos-15
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.core.outputs.ui-matrix) }}
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode 26
        run: sudo xcode-select -s /Applications/Xcode_26.0.app/Contents/Developer
      - name: Build app and UI tests once
        # Keychain tests need an ad-hoc signed app with an application identity;
        # shipping entitlements are unchanged. Build against the SDK before asking
        # for a device, as a fresh runner may not have registered its simulators yet.
        run: xcodebuild -project Supper.xcodeproj -scheme Supper -destination 'generic/platform=iOS Simulator' -derivedDataPath DerivedData CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual DEVELOPMENT_TEAM= CODE_SIGN_ENTITLEMENTS=Tests/UI/Simulator.entitlements build-for-testing
      - name: Wait for simulator readiness
        timeout-minutes: 5
        # Select the device only after the generic build has registered runtimes.
        # A booted device may still be migrating data or starting system apps;
        # launching XCTest then caused minute-long app-launch stalls/timeouts.
        run: xcrun simctl bootstatus 'iPhone 17 Pro' -b
      - name: Run UI regression tests in one session
        env:
          SUPPER_TEST_SUITE: ${{ matrix.suite }}
        run: |
          # One simulator/session per worker, with every test assigned exactly
          # once by the same source that creates the matrix.
          selection_text=$(python3 .github/scripts/ui_test_shards.py --suite "$SUPPER_TEST_SUITE")
          selection=()
          while IFS= read -r argument; do
            selection+=("$argument")
          done <<< "$selection_text"
          set +e
          xcodebuild -project Supper.xcodeproj -scheme Supper -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -parallel-testing-enabled NO -derivedDataPath DerivedData -resultBundlePath UIResults "${selection[@]}" test-without-building 2>&1 | tee UITestRun.log
          test_status=${PIPESTATUS[0]}
          set -e
          if [ "$test_status" -eq 0 ]; then exit 0; fi
          if [ "$test_status" -ne 65 ]; then exit "$test_status"; fi
          # Retry only known simulator lifecycle failures after a complete run.
          # Assertions, unknown errors and incomplete runs remain failures.
          retry_ids=$(python3 .github/scripts/simulator_retries.py UITestRun.log "${selection[@]}") || exit "$test_status"
          retry_selection=()
          while IFS= read -r test_id; do
            retry_selection+=("-only-testing:$test_id")
          done <<< "$retry_ids"
          echo "::warning::Retrying simulator lifecycle failures once; see UITestRun.log for the original results."
          xcodebuild -project Supper.xcodeproj -scheme Supper -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -parallel-testing-enabled NO -derivedDataPath DerivedData -resultBundlePath UIRetryResults "${retry_selection[@]}" test-without-building
      - name: Export UI screenshots
        if: always()
        run: |
          for result in UIResults UIRetryResults; do
            if [ -d "$result.xcresult" ]; then
              xcrun xcresulttool export attachments --path "$result.xcresult" --output-path "UITestAttachments/$result"
            fi
          done
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ui-test-results-${{ matrix.suite }}-${{ github.run_attempt }}
          path: |
            UITestAttachments
            UIResults.xcresult
            UIRetryResults.xcresult
            UITestRun.log
